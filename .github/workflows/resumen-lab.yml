name: Resumen LAB - actualizar mails

on:
  schedule:
    # cada 15 minutos (mas confiable que 5 min, que GitHub tiende a saltear)
    - cron: '*/15 * * * *'
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: resumen-lab-update
  cancel-in-progress: true

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 6
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Correr script de Resumen LAB
        env:
          GMAIL_USER: ${{ secrets.GMAIL_USER }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
        run: python scripts/resumen_lab.py

      - name: Commit y push si hubo cambios
        run: |
          git config user.name "resumen-lab-bot"
          git config user.email "actions@users.noreply.github.com"
          git add index.html
          if git diff --cached --quiet; then
            echo "Sin cambios para commitear."
          else
            git commit -m "Corrida automática $(date -u +'%Y-%m-%d %H:%M UTC')"
            git push
          fi
